# 查詢球隊列表 — E2E 操作流程

> 頁面：球隊管理（/teams）

## 元素定義

| 元素 | testid | 說明 |
|------|--------|------|
| 球隊頁面 | teams-page | 頁面容器 |
| 球隊列表 | team-list | 球隊表格 |

## 共用前置條件

- 「系統中有以下使用者：」→ 不需操作（mock 資料已預設）
- 「系統中有以下球隊：」→ 不需操作（mock 資料已預設）

## 規則：教練只能查詢自己建立的球隊

### 情境：教練查詢球隊列表

前置條件：
- 「教練 "coach1" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「教練查詢球隊列表」
  1. 前往球隊管理頁 → /teams
  2. 等待頁面載入 → #teams-page

預期結果：
- 「操作成功」→ #teams-page 文字可見
- 「回傳以下球隊：」→ #team-list 包含「紅龍隊」
- 「回傳以下球隊：」→ #team-list 包含「藍鷹隊」
- 「回傳以下球隊：」→ #team-list 不包含「白虎隊」

## 規則：管理者可查詢所有球隊

### 情境：管理者查詢球隊列表

前置條件：
- 「管理者 "admin" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「管理者查詢球隊列表」
  1. 前往球隊管理頁 → /teams
  2. 等待頁面載入 → #teams-page

預期結果：
- 「操作成功」→ #teams-page 文字可見
- 「回傳所有 active 狀態的球隊（共 3 筆）」→ #team-list 包含「藍鷹隊」
- 「回傳所有 active 狀態的球隊（共 3 筆）」→ #team-list 包含「紅龍隊」
- 「回傳所有 active 狀態的球隊（共 3 筆）」→ #team-list 包含「白虎隊」

## 規則：列表依建立時間倒序排列（最新在前）

### 情境：球隊列表排序

前置條件：
- 「教練 "coach1" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「教練查詢球隊列表」
  1. 前往球隊管理頁 → /teams
  2. 等待頁面載入 → #teams-page

預期結果：
- 「第一筆為 "紅龍隊"（較新建立）」→ #team-list 包含「紅龍隊」
- 「第二筆為 "藍鷹隊"（較早建立）」→ #team-list 包含「藍鷹隊」

## 規則：列表回傳欄位包含 ID、名稱、球員數量、建立時間、建立者

### 情境：確認回傳欄位

⏭️ 整個情境跳過
原因：API 層級驗證，E2E 不驗證回傳欄位結構

## 規則：支援分頁查詢，預設每頁 20 筆，可選 10/20/50 筆

### 情境：使用預設分頁

⏭️ 整個情境跳過
原因：API 層級驗證，E2E 不驗證分頁預設值

### 情境：指定每頁筆數

⏭️ 整個情境跳過
原因：API 層級驗證，E2E 不驗證分頁參數

## 規則：已刪除的球隊不顯示在列表中

### 情境：列表不包含已刪除球隊

前置條件：
- 「管理者 "admin" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「管理者查詢球隊列表」
  1. 前往球隊管理頁 → /teams
  2. 等待頁面載入 → #teams-page

預期結果：
- 「結果不包含 "黑豹隊"（已刪除）」→ #team-list 不包含「黑豹隊」

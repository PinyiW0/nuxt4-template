# 查詢訓練列表 — E2E 操作流程

> 頁面：訓練管理（/trainings）

## 元素定義

| 元素 | testid | 說明 |
|------|--------|------|
| 訓練頁面 | trainings-page | 頁面容器 |
| 訓練列表 | training-list | 訓練表格 |
| 新增訓練按鈕 | training-create | 開啟新增彈窗 |
| 刪除訓練按鈕 | training-delete | 刪除訓練 |

## 共用前置條件

- 「系統中有以下使用者：」→ 不需操作（mock 資料已預設）
- 「系統中有以下球隊：」→ 不需操作（mock 資料已預設）
- 「系統中有以下球員：」→ 不需操作（mock 資料已預設）
- 「系統中有以下訓練：」→ 不需操作（mock 資料已預設）

## 規則：訓練列表顯示今天及未來的訓練

### 情境：查詢訓練列表（今天是 2026-01-26）

前置條件：
- 「教練 "coach1" 已登入」→ （共用步驟，見 _common.flow.md）
- 「今天日期為 "2026-01-26"」→ 不需操作（mock 資料已預設）

操作步驟：
- 「教練查詢訓練列表」
  1. 前往訓練管理頁 → /trainings
  2. 等待頁面載入 → #trainings-page

預期結果：
- 「操作成功」→ 顯示成功提示
- 「回傳以下訓練：」→ #training-list 包含「王小明」
- 「回傳以下訓練：」→ #training-list 不包含「2026-01-20」

## 規則：教練只能查詢自己建立的訓練

### 情境：教練查詢訓練列表

前置條件：
- 「教練 "coach1" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「教練查詢訓練列表」
  1. 前往訓練管理頁 → /trainings
  2. 等待頁面載入 → #trainings-page

預期結果：
- 「結果不包含 coach2 建立的訓練」→ #training-list 不包含「李大華」

## 規則：管理者可查詢所有訓練

### 情境：管理者查詢訓練列表

前置條件：
- 「管理者 "admin" 已登入」→ （共用步驟，見 _common.flow.md）
- 「今天日期為 "2026-01-26"」→ 不需操作（mock 資料已預設）

操作步驟：
- 「管理者查詢訓練列表」
  1. 前往訓練管理頁 → /trainings
  2. 等待頁面載入 → #trainings-page

預期結果：
- 「回傳所有今天及未來的 active 訓練」→ #training-list 包含「王小明」
- 「回傳所有今天及未來的 active 訓練」→ #training-list 包含「李大華」

## 規則：列表依建立時間倒序排列（最新在前）

### 情境：訓練列表排序

前置條件：
- 「教練 "coach1" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「教練查詢訓練列表」
  1. 前往訓練管理頁 → /trainings
  2. 等待頁面載入 → #trainings-page

預期結果：
- 「依建立時間由新到舊排列」→ #training-list 包含「王小明」

## 規則：列表回傳欄位包含 ID、日期、受測選手、球隊、投球數、AI狀態、建立時間、建立者

### 情境：確認回傳欄位

⏭️ 整個情境跳過
原因：API 層級驗證，E2E 不需逐一檢查回傳欄位

## 規則：支援依日期範圍篩選

### 情境：篩選特定日期範圍

前置條件：
- 「教練 "coach1" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「教練查詢訓練列表，日期範圍 "2026-01-26" 至 "2026-01-31"」
  1. 前往訓練管理頁 → /trainings
  2. 等待頁面載入 → #trainings-page

預期結果：
- 「只回傳該日期範圍內的訓練」→ #training-list 包含「2026-01-26」
- 「只回傳該日期範圍內的訓練」→ #training-list 不包含「2026-01-20」

## 規則：支援分頁查詢，預設每頁 20 筆，可選 10/20/50 筆

### 情境：使用預設分頁

⏭️ 整個情境跳過
原因：API 層級分頁邏輯，E2E 不驗證

## 規則：已刪除的訓練不顯示在列表中

### 情境：列表不包含已刪除訓練

前置條件：
- 「教練 "coach1" 已登入」→ （共用步驟，見 _common.flow.md）

操作步驟：
- 「教練查詢訓練列表」
  1. 前往訓練管理頁 → /trainings
  2. 等待頁面載入 → #trainings-page

預期結果：
- 「結果不包含已刪除的訓練」→ #training-list 不包含「2026-01-15」

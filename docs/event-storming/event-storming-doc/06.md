# 從 Gherkin 到測試程式碼：事件風暴部位的直接翻譯

各位規格驅動全自動開發術的魔法初學者們好！

上一個單元學完 TDD 的三步驟（Red-Green-Refactor），現在你知道要先寫測試了。但問題來了：

**你會寫測試嗎？從 Gherkin 規格要怎麼翻譯成測試程式碼？**

這就是本單元要解決的核心問題！水球我會教你如何用**映射思維**（意思就是...照著規則對應翻譯就對了），輕鬆將 Gherkin 語句轉換成高品質的測試程式碼。

在第二章「事件風暴 → Gherkin」的翻譯中，我們已經完成了從事件風暴部位（Aggregate、Command、Query、Event）到 Gherkin 語句的精準映射。

那麼，在「Gherkin → 測試程式碼」這一步，咱們就能直接從 Gherkin 識別事件風暴部位，然後一步到位翻譯成測試程式碼。

## 一、如何翻譯 Gherkin 到測試程式碼？

### 承接第二章：事件風暴 → Gherkin 已完成映射

還記得第二章的映射公式嗎？

**修改型操作**：

**查詢型操作**：

在「事件風暴 → Gherkin」這一步，咱們已經完成了精細的映射，每個事件風暴部位都對應到特定的 Gherkin 語句位置。

那現在要進入「Gherkin → 測試程式碼」這一步，你可能會想：「我要怎麼寫測試？」

別擔心，水球我會直接給你翻譯規則！

## 二、映射規則總覽

開始詳細講解之前，先看完整的映射關係：

**名詞說明**： 通常無論是前後端都會有所謂的「分層設計」，在實作自動化測試的時候就要考慮實踐分層，這篇文我所教你的測試策略會基本上把受測系統分層兩層：

**注意**：本文不涵蓋 Event 的處理。Event 驗證是進階用法，涉及 Event Sourcing 和 CQRS 架構，建議初學者先掌握上述基礎映射後，再學習 Event 相關的測試技巧。

### 2.1 後端分層架構與測試策略

開始翻譯 Gherkin 之前，我們要先理解業務軟體的分層架構。這對於理解「為什麼要這樣翻譯」非常重要。

#### 後端的三層架構

一般的業務軟體後端會分成三層：

**各層職責**：

> **註：在不同程式語言的環境中，可能會有不同分層的命名慣例，好比說也有人將 Repository 稱之為 Data Access Layer。Anyway，在本文中我們通稱資料存取層為 Repository。**

#### 為什麼要分層？

**單一職責原則**——意思就是，每一層只做一件事，這樣當需求變動時，只需要修改對應的層級。

好比說（簡化來說）：

#### 測試策略：Test → Service → Repository

在 TDD 中，我們的測試策略是這樣的：

**為什麼 Given 和 Then 都用 Repository？**

這種策略確保我們測試的是 **Service 的業務邏輯**，而不是 Repository 的資料存取邏輯。

如此一來，**就可以用自動化測試來逼迫 AI 把業務邏輯實作正確**，業務邏輯是最重要的，你需要確保它正確無誤。

#### 前端的類似架構

如果你是前端工程師，前端也有類似的分層架構：

**前端測試策略：Test → Component → API**

### 2.2 翻譯範例：從 Gherkin 到測試程式碼

理解了分層架構後，接下來我們要學習**如何將 Gherkin 的每一句話翻譯成測試程式碼**。

在第二章中，我們學習了「事件風暴 → Gherkin」的翻譯。現在要進入「Gherkin → 測試程式碼」這一步，**關鍵就是識別出每句 Gherkin 對應的事件風暴部位**，然後套用該部位的翻譯規則。

#### 單句翻譯範例

好比說，當你看到這句 Gherkin：

翻譯步驟：

> **本文下面會講得非常清楚，會教你如何每個事件風暴部位怎麼精準翻譯成對應的測試程式碼，這裡不急，先感受一下就好，想要查找規則細節時再往下讀。**

#### 完整 Example 的翻譯

現在讓我們看一個完整的 Example（Given-When-Then）如何翻譯成測試程式碼：

**原始 Gherkin**：

**翻譯後的測試程式碼**：

**映射關係（簡化版）**：

#### 本文的編排邏輯

看到了嗎？只要識別出 Gherkin 語句對應的事件風暴部位，就能套用對應的翻譯規則。

因此，本文接下來會按照「事件風暴部位」來組織章節：

**把這些章節當成「查字典」的工具書**——當你遇到特定的 Gherkin 語句不知道怎麼翻譯時，就回來查對應部位的章節。

> **當然，寫成 Prompt 就可以讓 AI 幫你完成每一條翻譯規則。但你如果能把細節都記清楚、用雙手實踐過一遍，才能真正內化，才有辦法針對情境，客製化適合的 Prompt。**

準備好了嗎？讓我們開始深入學習各部位的翻譯規則！

## 三、Aggregate：翻譯規則

### 識別規則

當 Gherkin 語句描述**實體的狀態或存在**時，識別為 Aggregate。

**Aggregate 與 DBML 的關係**：

所有的 Aggregate 都應該事先在 **DBML Spec** 中被定義成資料表。DBML（Database Markup Language）是用來定義資料庫結構的語言，它清楚描述了每個資料表的：

**為什麼需要 DBML？**

在翻譯 Gherkin 時，你需要參考 DBML 來：

### 在 Given 中：創建初始狀態

**範例**：

**翻譯步驟**：

**DBML 範例**：

**生成測試程式碼 (Python)**：

**邏輯說明**：

### 在 Then 中：驗證最終狀態

**範例**：

**翻譯步驟**：

**生成測試程式碼 (Python)**：

**邏輯說明**：

### 關鍵原則

#### 1. 必須參考 DBML

在創建和驗證 Aggregate 時，必須參考 DBML 來確定：

#### 2. 中文狀態值 → 英文 enum

Gherkin 通常用中文描述，但程式碼使用英文 enum：

**如何判斷對應？參考 DBML 的 note！**

#### 3. 使用同一個 repository 實例

Given 和 Then 必須使用同一個 repository 實例，確保 Service 和測試共享資料源。

## 四、Command：翻譯規則

### 識別規則

當 Gherkin 語句描述**動作或操作**時（可為完成式描述），識別為 Command。

### 翻譯方式（Given 和 When 相同）

**處理方式完全相同**：調用 `service.method(...)`

唯一的差別是**用途**：

### 範例 1：在 Given 中建立前置狀態

**生成測試程式碼 (Python)**：

**邏輯說明**：

### 範例 2：在 When 中執行操作

**生成測試程式碼 (Python)**：

**邏輯說明**：

## 五、Query：翻譯規則

### 識別規則

當 When 語句包含「查詢」「取得」「獲取」等關鍵字時，識別為 Query。

### 範例

**翻譯步驟**：

**生成測試程式碼 (Python)**：

**邏輯說明**：

### Query 與 Command 的差異

Query 必須接收回傳值，供後續 Then 驗證。

## 六、Read Model：翻譯規則

### 識別規則

當 Then 語句驗證「查詢結果」或「回傳值」時，識別為 Read Model。

### 完整範例

**翻譯步驟**：**識別要驗證的欄位** - `progress=80`, `status="進行中"`

**生成測試程式碼 (Python)**：

**邏輯說明**：

## 七、操作成功/失敗：翻譯規則

### Then 第一句的識別

### 成功場景範例 (Python)

**邏輯說明**：

### 失敗場景範例 (Python)

**邏輯說明**：

## 八、完整實戰範例：增加影片進度

讓我們用一個完整的範例，展示如何逐句翻譯 Gherkin 成測試程式碼。

### 8.1 範例：成功更新進度

**Feature File**：

**逐句翻譯**：

#### 第一句：Given 用戶 "Alice" 在課程 1 的進度為 70%，狀態為 "進行中"

**參考 DBML**：

**生成測試程式碼 (Python)**：

#### 第二句：When 用戶 "Alice" 更新課程 1 的影片進度為 80%

**生成測試程式碼 (Python)**：

#### 第三句：Then 操作成功

**生成測試程式碼 (Python)**：

#### 第四句：And 用戶 "Alice" 在課程 1 的進度應為 80%，狀態應為 "進行中"

**生成測試程式碼 (Python)**：

### 8.2 範例：進度倒退失敗

**Feature File**：

**完整測試程式碼 (Python)**：

### 8.3 範例：進度達到 100% 完成課程

**Feature File**：

**完整測試程式碼 (Python)**：

## 九、總結

透過本文，我們學習了如何將 Gherkin 規格直接映射到測試程式碼。

### 核心映射原則

### 關鍵要點

1. **識別事件風暴部位**：從 Gherkin 語句特徵判斷是 Aggregate、Command、Query 還是 Read Model

2. **參考 DBML**：處理 Aggregate 時，必須參考 DBML 確定屬性、型別、複合 Key 和 enum 值

3. **依賴注入**：Given 和 Then 必須使用同一個 repository 實例，透過依賴注入讓 Service 和測試共享資料源

4. **中文到英文**：Gherkin 的中文狀態值需對應到程式碼的英文 enum（參考 DBML note）

5. **成功與失敗**：

掌握這些映射規則，你就能系統化地將任何 Gherkin 規格翻譯成高品質的測試程式碼！

下一個單元，水球我就要送你一系列的 AI x TDD 的 Prompt 來把上述每一個步驟透過 AI 輔助精準做到啦！

Let's Go！
